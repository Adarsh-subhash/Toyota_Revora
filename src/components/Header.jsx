import React from "react";

export default function Header({ currentUser, searchQuery, setSearchQuery, togglePortalQuickly, onShowToast }) {
  return (
    <header className="workspace-header" style={{ justifyContent: "flex-end", padding: "12px 24px", minHeight: "56px" }}>
    </header>
  );
}
